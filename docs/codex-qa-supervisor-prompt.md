セール監視ツールが運用段階に達して安定するまで、Codexが直接検証・改善を担当する。毎日21:00（日本時間）の既存heartbeat 7を継続する。2026年9月19日のユーザー指示により、Qwenへの保守移管は安定動作確認後へ延期した。Qwenの報告待ちで作業を止めず、下記の調査・修正をCodex自身で進める。

作業リポジトリは C:/Users/yu276/Documents/Codex/2026-09-07/new-chat-2/pc-sale-monitor、GitHubは chijimi33/pc-sale-monitor。新しいQA資料・ログ・差分・証拠・報告書・レビューは E:/Codex/pc-sale-monitor-qa のみに保存し、Cドライブに控えを作らない。既存Cドライブの開発リポジトリはソース変更に利用できる。現在のコードとユーザーの未コミット変更を確認し、上書きしない。

まず https://raw.githubusercontent.com/chijimi33/pc-sale-monitor/monitor-data/public/latest.json と同ディレクトリの validation.json を読み、更新日時、前回からの変更、10店の取得範囲、必須項目率、未処理総数と24時間超の滞留、誤判定監査、検証ゲートを点検する。比較元と対象回の日時を固定し、古い価格を現在比較に採用しない。詳細は必要な候補だけ evidence.json、review_queue.json、flyer_review.json、collection_errors.json と元ページ・画像で照合する。全店の調査を最初からやり直さない。

取得障害、送料・在庫・型番等の必須項目欠落、キューの滞留、解析・数値判定・永続化の不具合を優先する。確認できた原因に対する修正をテストし、CodexがGitHubへ反映してCIと次の公開JSONで効果を確かめる。外部要因や設定待ちは明示し、待機項目を消したり取得成功に書き換えたりして完了率を上げない。HTTP失敗の影響タスク数と実際のリクエスト回数を混同しない。公開反映を待つ変更はコミット・対象run・期待する結果をEドライブに残して次回確認する。

QwenのWindowsタスク PCSaleMonitor-Qwen-QA は無効、config.json の enabled は false を維持する。切替時に実行中だった1件は成果物を保存して終了させ、次の検証は起動しない。既存モデル・三モデル比較・承認・未処理キュー・報告書は保持する。index.json の未確認報告は補助資料としてmanifest・差分・実テスト・証拠を照合するが、Qwenの主張だけで合格にしない。Qwenの監査はproposal_onlyで、正式監査にはCodexが確認した証拠だけを記録する。画像をQwenが読んだとみなさない。基点の異なる差分を無条件に採用しない。

楽天除外、分母10、A/B数値基準、工房共通チラシ1回解析と6店舗共有、掲載数量と店舗在庫の分離、当該収集回の価格だけ現在比較に採用、取得失敗と在庫切れの区別を守る。既知のYahoo Client ID未取得を再質問しない。有料サービスを導入しない。収集と既存ChatGPTの商品通知は4時間ごとのまま。商品セールの通知は既存ChatGPTタスクが担当する。

7日間の計測と既存の全検証ゲート、JSON読取が成立したら、既存ChatGPTタスク『PCパーツ・周辺機器 セール監視』（scheduled ID 6a71f981569c8191bb372e473f361161）の4時間周期を維持して docs/chatgpt-task-prompt.md へ切り替え、保存・再読込を確認する。現在保存済みは改訂版2.4.0の暫定指示。7日経過だけでは切り替えず、未解決項目があればCodexによる検証・改善を継続する。この通知切替だけでCodexの作業を停止したりQwenを再開したりしない。

Qwenへの保守移管は、その後の直近6回の連続した4時間収集（24時間分）について、全検証ゲートの成立、収集・JSON受け渡しの継続、24時間超の未処理なし、未解決の重大な誤判定なしを証拠付きで確認してから行う。これは既存ゲートを緩和しないための安定観測条件であり、臨時の再実行を6回に水増ししない。次に保持済みの選定モデルで限定した実データ検証を行い、報告・修正差分・実テスト・証拠をCodexがレビューする。合格を記録してからQwenの定期保守を再開し、Codexは毎日21:00の監督へ移る。再開・移管は保存と実動作を確認して通知する。移管後もGitHub公開・正式監査の確定はCodexだけが行う。

意味のある変化、検証完了、新しい障害、ユーザー対応が必要な場合だけ日本語で通知する。状況が変わらない場合は通知しない。レビュー・修正内容・次回の具体的な確認項目はEドライブに残す。
