# 導入状況（2026年9月14日）

## 稼働済み

- [GitHubリポジトリ](https://github.com/chijimi33/pc-sale-monitor) に配置済み。
- 10店の独立したActionsジョブが4時間ごとに収集し、`monitor-data` ブランチへ履歴・未処理・イベント・公開JSONを保存。
- [初回実行](https://github.com/chijimi33/pc-sale-monitor/actions/runs/34777273580) の集計・公開が完了。サイト側取得失敗はデータ内の状態として別に計測する。
- 既存ChatGPTタスク「PCパーツ・周辺機器 セール監視」は改訂版 `2.4.0-ten-stores-shared-flyer-parallel` を保存・再読込確認済み。楽天除外、Web呼出数上限撤去、共通チラシの扱いを修正し、4時間ごとの実行を維持。
- 外部JSONへの通知切替は未実施。計測開始は2026-09-14 04:28 JST、7日経過は最短2026-09-21 04:28 JST。経過時間だけでは切り替えない。

## 初期計測で判明した課題

2026-09-14 14:28 JSTの公開結果では巡回完了2/10店。Actionsが成功しても、全店の価格取得成功を意味しない。最新の結果は [最新索引](https://raw.githubusercontent.com/chijimi33/pc-sale-monitor/monitor-data/public/latest.json) と [検証結果](https://raw.githubusercontent.com/chijimi33/pc-sale-monitor/monitor-data/public/validation.json) を参照する。

| 対象 | 確認した課題・対応 |
|---|---|
| Yahoo! | ユーザー回答はClient ID未取得。設定待ち。履歴や現在価格を捏造せず、取得率の不足として記録 |
| Joshin / Sofmap | 403応答。取得できた商品だけ保持し、取得失敗を在庫切れにしない |
| ツクモ | ローカルでは取得できたがActionsから接続切断。原因を記録して次回再試行 |
| ビック / ヨドバシ | タイムアウトなど。ヨドバシは表示後取得も確認したがプロトコルエラーが残る |
| Amazon | 商品によって購入欄・状態・販売者同一性・送料の確認不足。ちもろぐ価格で補完しない。初回送料無料を一般送料に含めない修正済み |
| ark / 工房通販 | 一部の階層の深いセールページが未処理。入れ子のセールへの巡回とarkのBTO候補検出を追加。BTO構成の同一性は確認待ち |
| 工房チラシ | 共通版を一度解析。OCR結果と画像URLを `flyer_review.json` にまとめて確認へ渡す。掲載数量は店舗在庫へ変換しない |

共通基盤では、同一巡回内の失敗ページの重複取得を抑止し、今回未取得の前回価格を現在比較から除外した。数値境界、誤同定、履歴・再開、共通チラシ、条件付き送料を含むローカル48テストが合格。

## 切替までに行うこと

取得障害・必須項目の不足・24時間超の未処理を点検し、確認できるサイトの解析を修正する。証拠照合による誤判定レビューを記録する。7日間の検証ゲートとJSONの読取を確認した後、既存タスクの指示を `docs/chatgpt-task-prompt.md` に切り替える。Yahoo!の設定やサイト側の制限が未解決なら、取得範囲が不足していることを明示して並行検証を続ける。

## 9月15日の確認と修正

9月15日00:35 JSTの公開結果（run `34859251880-1`）は4/10店の巡回完了。arkと工房通販でセール一覧の取得漏れが解消した。計測は6回、切替条件は未達。ジョブ成功でもAmazonの販売条件不足や各店の403・接続断は残っている。

ツクモでは前回取得した806商品が今回再確認されず、未処理1,282件の中で失敗した比較一覧151件を先に処理していた。現在価格の再確認を比較検索より先にし、新着などの優先度を検索から一覧・商品へ引き継ぐよう修正。実際の保存キューで、先頭25件がセール一覧1件・セール商品24件になることを確認した。これは処理順の改善であり、サイトの接続障害の解消を意味しない。未処理は削除しない。

共通チラシのパーツ面（ハッシュ `8cb8a341…`）を元画像で確認し、22商品の価格・販売開始日・全店合計数量・購入制限を登録した。全店合計数量は対象6店舗の在庫と解釈しない。JAN・明記のない状態・一部の完全型番は未確認のまま保持する。DDR4 16GBの型番表記の不一致も保留。PC本体面は未確認なので、版全体を確認済みにしない。

OCRの唯一の価格候補だった「月々3,000円」は分割払い広告であり、商品候補から除外した。22商品のチラシ転記確認は、A/B判定の誤通知監査20件の代用にはしない。実データの集計で、22個の一意な商品、店舗在庫未確認、未確認商品の通知0件を確認した。単体テストは52件合格。

## 9月15日午後の確認と修正

14:27 JSTの公開結果（run `34930438094-1`）で、前回のチラシの部分確認・分割払い広告の除外が反映された。ツクモの商品再確認は先に実行されるようになったが、接続断が継続しており価格は未取得。arkにアクセス制限が発生し、巡回完了は3/10店。24時間超の未処理はビック299件、Joshin299件、Sofmap3件、Yahoo!302件、ヨドバシ298件、合計1,201件。既存の待ち行列は保持する。

従来は `Retry-After` の長い待機が1URLだけに適用され、その後に同じホストの別URLを要求できていた。待機期限をホスト単位で共有・永続化し、HTTP取得とブラウザー取得の両方で守るよう修正した。最終再試行で返った待機期限も保存し、期限が過ぎると再開できる。他ホストへの収集は止めない。公開索引にはUTCのUnix秒で `retry_after_epoch_seconds` を追加する。

7日間の検証は、同じ4時間枠の手動再実行・コード反映を重複して数えないようにした。実行数とは別に `measured_four_hour_windows` を記録し、取得率は各枠の最新結果で集計する。A/B基準は維持。短時間の50回再実行では期間検証を通過せず、40個の異なる4時間枠と他条件を満たした場合は通過できることをテストした。全58テスト合格。実際の10店の取得率・履歴期間・誤判定監査はまだ切替条件を満たしていない。

## 9月15日夜の証拠照合と修正

17:15 JST公開の run `34942803104-1` でツクモの今回取得が910商品まで回復した。巡回完了は2/10店で、24時間超の未処理は合計1,198件。10回の実行は8個の4時間枠に相当し、通知切替の条件は未達。

ツクモの比較検索に、HTTP 404でも「該当する商品がありませんでした」と表示する正常な検索結果があった。実ページの検索語・タイトル・専用表示を照合して空結果を保存し、この場合だけ未処理を完了するよう修正した。通常の404は取得失敗のまま保持し、空検索から在庫切れを作らない。ブラウザーで同じ空検索を繰り返さない。

通知候補1件（イベント `dd992baf0c0c85e7bc3be81a8f22eae4`）を3店の元ページで照合した。同一JANの価格・送料無料・在庫は確認できたが、arkの構造化データの期限は9月18日、対象商品の価格欄は10月1日23:59で不一致。期限を断定せず、根拠を保存して要確認とする。年のない表示から新しい期限を推測しない。保存済みイベントも次回の集計で現在条件を再評価するため、この不一致が続く間は通知候補から外れる。

この1件は監査着手済み・未解決であり、「問題なし20件」の実績には加算しない。未解決の監査指摘が残る間は切替できない。実ページで空検索の検出と期限不一致の保留を確認し、単体・集計を含む63テストで履歴保持、通知保留、通常の404、関連商品の期限を取り込まないことを検証した。

## 9月16日未明の反映確認と索引軽量化

前回修正直後の run `34972430855-1` で、ツクモの空検索160件が正常終了した。次の run `34975549339-1`（9月15日23:13 JST公開）では、ツクモの接続断が再発し今回取得0件。空検索の成功件数は巡回ごとに集計するため、この回の0件は前回の160件の処理が失われた意味ではない。巡回完了2/10店、24時間超の未処理2,589件、9個の4時間枠を計測した。取得失敗を前回価格で埋めない。

期限不一致で監査中の商品は `review_queue.json` に移り、通知候補0件となった。同じ不一致のあるark商品は118件あり、値引きが確定しても期限を誤って断定しないよう要確認を維持する。監査の未解決1件も `validation.json` へ反映済み。

最新索引には同種の失敗が多数のURL行として繰り返されていた。原因別の件数と最大3件のURL例を索引にまとめ、URL別の全件を `collection_errors.json` へ分離する。これは表示用の例示数で、Web取得回数を制限しない。状態・未処理・失敗履歴は保持する。

上記の公開結果を再整形し、索引を171,210バイトから25,723バイトへ約85％削減した。URL別840行・影響した処理3,150件は詳細に保存し、件数の一致を検証した。元の確認日時とrun IDは維持し、再整形を新しい巡回や計測枠に数えない。64テスト合格。7日間の期間・取得率・滞留・監査の切替条件は引き続き未達。

## 9月16日午後の確認とドスパラの商品識別修正

最新公開は9月16日08:28 JSTの run `35033094270-1`。定期実行にも軽量な索引とURL別詳細の分離が反映された。巡回完了2/10店、24時間超の未処理3,095件、15回・11個の4時間枠を計測。ツクモの接続断、各店の403・待機制限、Yahoo!の設定待ちは継続している。チラシの版・部分確認状態に変更はない。

ドスパラの識別可能商品は31/40件だった。確認済み商品名にメーカーと型番の記載があっても、メーカー名の固定リストに含まれない場合に識別できなかった。ADATA・Razer・エレコムの元ページ3件を取得し、対象商品の見出しでメーカーと商品名の間に2つの空白があることを確認した。この区切りを空白正規化の前に読み取り、商品表記全体を保存するよう修正した。既存の明示されたメーカー型番は上書きしない。

実ページ3件でメーカー名と型番を含む完全な商品表記を取得できた。JANは未記載のままnullを維持する。限定型番の末尾、容量、色、括弧内の製品番号を削除せず、短いシリーズ名との同一視をしない。単語間の空白だけでメーカーを推測すること、別商品の見出しの採用、複数の候補見出しからの恣意的な選択は避ける。2枚組の構成確認は引き続き保留。全66テスト合格。本番の識別率改善は修正後の収集結果で別途確認する。
